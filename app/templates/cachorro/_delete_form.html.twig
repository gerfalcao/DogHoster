<form method="post" action="{{ path('app_cachorro_delete', {'id': cachorro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cachorro.id) }}">
    <button class="btn">Delete</button>
</form>
