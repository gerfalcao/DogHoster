{% extends 'base.html.twig' %}

{% block title %}Login de usuário{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
<div class="item-show">
    <div class="card4">
    <form action="{{ path('app_login') }}" method="post">
        <label for="basic-url">Email</label>
        <input type="text" class="form-control" placeholder="exemplo@email.com" id="username" name="_username" value="{{ last_username }}">
        <br>
        <label for="password">Senha:</label>
        <input type="password" class="form-control" placeholder="*****" id="password" name="_password">

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"> #}
        <br>
        <button class="btn btn-primary" type="submit">login</button>
    </form>
    </div>
</div>
{% endblock %}
